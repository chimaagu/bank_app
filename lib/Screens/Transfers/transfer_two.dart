import 'package:flutter/material.dart';

class TransferTwoPage extends StatelessWidget {
  const TransferTwoPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("Second Stage"),
      ),
    );
  }
}
